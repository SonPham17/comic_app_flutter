import 'package:flutter/cupertino.dart';

class BackgroundStyleModel{
  IconData iconData;
  bool isSelected;
  Color textColor;
  Color backgroundColor;

  BackgroundStyleModel({this.iconData,this.isSelected,this.textColor,this.backgroundColor});
}